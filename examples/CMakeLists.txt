FILE(GLOB SRC_EXAMPLE_APPS RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} *.cpp)
FOREACH(APP_SRC ${SRC_EXAMPLE_APPS})
	STRING(REPLACE ".cpp" "" APP_NAME ${APP_SRC})
	ADD_EXECUTABLE(${APP_NAME} ${APP_SRC})
	TARGET_LINK_LIBRARIES(${APP_NAME} git2ppp)
ENDFOREACH()
